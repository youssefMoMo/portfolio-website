/**
 * SmartImage.jsx — DELETED ✗
 *
 * This file has been eradicated from the asset pipeline. Any remaining
 * `import SmartImage` statement will throw immediately at runtime, surfacing
 * the stale reference so it can be migrated to SafeImage.
 *
 * Migration:
 *   Before:  import SmartImage from "@/components/SmartImage";
 *   After:   import SafeImage  from "@/components/SafeImage";
 *            // or: import { SafeImage } from "@/components/SafeImage";
 *
 * Delete this file once all imports have been updated.
 */

throw new Error(
  "[SmartImage] This component has been removed. " +
  "Import SafeImage from \"@/components/SafeImage\" instead."
);
